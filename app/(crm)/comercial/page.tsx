import { Briefcase } from "lucide-react"
import { EmBreve } from "@/components/simple/em-breve"

export default function ComercialPage() {
  return (
    <EmBreve
      titulo="Comercial"
      icon={Briefcase}
      descricao="Padronização de vendas: scripts de abordagem, respostas prontas, objeções, propostas padrão, modelos de contrato e precificação."
    />
  )
}
