import { Megaphone } from "lucide-react"
import { EmBreve } from "@/components/simple/em-breve"

export default function MarketingPage() {
  return (
    <EmBreve
      titulo="Marketing da SIMPLE"
      icon={Megaphone}
      descricao="O crescimento da própria SIMPLE: ideias de posts, roteiros, vídeos brutos, cases, bastidores e calendário de conteúdo."
    />
  )
}
